language: node_js
node_js:
  - "10.13.0"

script:
  - npm run build
  - npm run coverage

before_install:
  - npm i -g babel-preset-env mocha chai babel-cli istanbul coveralls

  after_script:
  - ./node_modules/coveralls/bin/coveralls.js < ./coverage/lcov.info  && npm run clean
